-- Отсечка действительности выданных токенов.
--
-- Смена пароля не закрывала прежние сессии: угнанный вход оставался
-- рабочим до тридцати суток, и сброс пароля — единственное, что
-- предлагает интерфейс, — ничего не отзывал.
--
-- Колонка добавляется пустой, а не «сейчас»: у тех, кто уже работает,
-- отсечки нет, и обновление не выбрасывает их из трекера.
ALTER TABLE "User" ADD COLUMN "sessionsFrom" DATETIME;
