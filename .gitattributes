# Скрипты для сервера всегда с переводами строки Unix. Виндовые CRLF
# ломают строку запуска: bash ищет интерпретатор "/usr/bin/env bash\r"
# и сообщает о нём так, что причину видно не сразу.
*.sh text eol=lf
